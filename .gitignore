node_modules/
dist/
coverage/
*.tsbuildinfo

# Estado derivado del harness: siempre reconstruible con `valmen index`.
.valmen/state/

# Secretos: nunca versionar credenciales ni configuración local.
.env
.env.local
*.local.yaml

# macOS
.DS_Store

# La configuración de opencode lleva rutas absolutas de esta máquina: el
# ejecutable del servidor MCP. Se genera con `valmen mcp --install`.
opencode.json
