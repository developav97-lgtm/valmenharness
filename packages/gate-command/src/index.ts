/**
 * @valmen/gate-command — evaluador de gates por comando.
 *
 * El evaluador que hay que intentar primero: determinista, sin coste y sin red.
 * Un gate que se puede decidir con un script no debe gastar una llamada a un
 * modelo.
 */

export * from "./command.js";
